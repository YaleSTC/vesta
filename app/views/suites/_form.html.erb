<%= simple_form_for [@building, @suite] do |f| %>
  <%= f.input :number %>
  <% if @building %>
    <%= f.hidden_field :building_id, value: @building.id %>
  <% else %>
    <%= f.association :building %>
  <% end %>
  <%= f.input :medical, label: 'Medical suite' %>
  <%= f.submit %>
<% end %>
