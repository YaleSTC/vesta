<%= simple_form_for [@building, @suite, @room] do |f| %>
  <%= f.input :number %>
  <%= f.input :beds %>
  <%= f.input :suite_id, as: :select, collection: Suite.all, label_method: :name %>
  <%= f.submit %>
<% end %>
