<%= simple_form_for [@draw, @group], url: send_invites_draw_group_path(@draw, @group) do |f| %>
  <%= f.input :invitations, label_method: :full_name, collection: @students, input_html: { multiple: true } %>
  <%= f.button :submit, 'Send Invitations' %>
<% end %>
