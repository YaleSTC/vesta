<% provide(:title, 'Results by Suite') %>
<h1>Results by Suite</h1>
<%= render partial: 'suites_table', locals: { suites: @suites } %>
<%= link_to 'Export CSV', export_results_path, class: 'button' %>
