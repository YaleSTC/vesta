<% provide(:title, 'All Draws') %>
<h1>All Draws</h1>
<%= render partial: 'table', locals: { draws: @draws } %>
<% if policy(Draw).new? %>
  <%= link_to 'New draw', new_draw_path, class: 'button' %>
<% end %>
